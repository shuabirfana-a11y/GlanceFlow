# Annotations

Copy `../annotation_template.json` to `<sample_id>.json`, annotate independently from predictions, and require a second human reviewer before setting `annotation_status` to `APPROVED`.
