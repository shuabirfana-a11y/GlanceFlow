# Local result staging

Canonical generated Stage 13 outputs are written to `outputs/real_world/`. This directory is reserved for reviewed dataset-local notes and contains no measurements while status is `WAITING_FOR_DATA`.
